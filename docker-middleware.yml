version: '3'

services:
  composer:
    image: composer:latest
    working_dir: /app
    entrypoint: composer
    command: --help
    volumes:
      - ..:/app:cached
      - /app/.git
      - /app/docker
      - /app/node_modules
  npm:
    image: node:12.22.3
    working_dir: /app
    entrypoint: npm
    command: -h
    volumes:
      - ..:/app:cached
      - /app/.git
      - /app/docker
      - /app/vendor
